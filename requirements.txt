# Qwen3-TTS Application Dependencies

# Core TTS library
qwen-tts

# Deep learning framework - MUST INSTALL CUDA VERSION
# First uninstall CPU version if installed:
#   pip uninstall torch torchvision torchaudio
# Then install CUDA version (choose based on your CUDA version):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Or for CUDA 11.8:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# torch>=2.0.0  # Do NOT use default pip install - it gives CPU version

# Audio processing
soundfile
sox  # Required for audio processing

# Web UI framework
gradio>=4.0

# Model downloading
huggingface_hub

# Optional: ModelScope mirror for China users
# pip install modelscope

# Optional: Flash Attention 2 for faster inference
# Requires CUDA and compatible GPU, Python 3.10+
# pip install flash-attn --no-build-isolation
